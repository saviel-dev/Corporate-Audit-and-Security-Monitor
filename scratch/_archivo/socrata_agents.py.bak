import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))
from app.services.co_discovery import run_co_discovery, DEFAULT_TERMS

if __name__ == "__main__":
    res = run_co_discovery(DEFAULT_TERMS)
    for r in res:
        print(f"CO | {r.get('agent_name', '')}")
